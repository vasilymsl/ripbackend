# Конфигурация веб-сервера
ServiceHost = "0.0.0.0"
ServicePort = 8080

# Конфигурация базы данных PostgreSQL
[database]
host = "localhost"
port = 5432
user = "myuser"
password = "mypassword"
dbname = "credits"
sslmode = "disable"

# Конфигурация MinIO
[minio]
endpoint = "localhost:9000"
access_key = "minio"
secret_key = "minio124"
use_ssl = false
bucket_name = "credits"

