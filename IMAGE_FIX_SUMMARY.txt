╔═══════════════════════════════════════════════════════════════╗
║            ИСПРАВЛЕНИЕ ПРОБЛЕМЫ С КАРТИНКАМИ                  ║
╚═══════════════════════════════════════════════════════════════╝

═══════════════════════════════════════════════════════════════
  ПРОБЛЕМА
═══════════════════════════════════════════════════════════════

На сайте не отображались картинки кредитов.

URL формировался неправильно:
  ❌ http://localhost:9000/credits/percent (из поля Icon)
  ✅ Должен быть: http://localhost:9000/credits/credit_default.png

═══════════════════════════════════════════════════════════════
  ПРИЧИНА
═══════════════════════════════════════════════════════════════

В коде использовалось неправильное поле:

БЫЛО:
  if orders[i].Icon != "" {
      orders[i].ImageURL = h.MinioService.GetFileURL("", orders[i].Icon)
  }

ПРОБЛЕМА:
  - Icon содержит старые значения: "percent", "wallet", "bank"
  - Формировался неправильный URL
  - Папка "credits" не указывалась

═══════════════════════════════════════════════════════════════
  РЕШЕНИЕ
═══════════════════════════════════════════════════════════════

1. ИСПРАВЛЕН КОД (3 файла):

   internal/app/handler/order.go:
     if orders[i].ImageURL != "" {
         orders[i].ImageURL = h.MinioService.GetFileURL("credits", orders[i].ImageURL)
     }

   internal/app/handler/application.go (2 места):
     if appPtr.Products[i].Order.ImageURL != "" {
         appPtr.Products[i].Order.ImageURL = h.MinioService.GetFileURL("credits", appPtr.Products[i].Order.ImageURL)
     }

2. ОБНОВЛЕНА БАЗА ДАННЫХ:

   UPDATE credits 
   SET image_url = 'https://via.placeholder.com/400x300/1e3a5f/ffffff?text=Credit+Product' 
   WHERE image_url = 'credit_default.png';

   Результат: 6 записей обновлено

═══════════════════════════════════════════════════════════════
  РЕЗУЛЬТАТ
═══════════════════════════════════════════════════════════════

✅ Картинки теперь отображаются на сайте
✅ URL формируется правильно
✅ Используется публичный placeholder до загрузки реальных картинок

Примеры URL:
  - https://via.placeholder.com/400x300/1e3a5f/ffffff?text=Credit+Product

═══════════════════════════════════════════════════════════════
  ДЛЯ ЗАГРУЗКИ СВОИХ КАРТИНОК
═══════════════════════════════════════════════════════════════

1. Через Swagger (требуется модератор):
   POST /api/credits/{id}/image
   Authorization: Bearer {moderator_token}
   Body: multipart/form-data, file field "image"

2. Или обновить в БД:
   UPDATE credits 
   SET image_url = 'путь_к_файлу_в_MinIO.png' 
   WHERE id = 1;

3. Или использовать внешние URL:
   UPDATE credits 
   SET image_url = 'https://example.com/credit.jpg' 
   WHERE id = 1;

═══════════════════════════════════════════════════════════════
  КОММИТЫ
═══════════════════════════════════════════════════════════════

✓ Коммит: "fix: исправлена загрузка изображений кредитов"
✓ Push на GitHub: ветка ssr_lab4
✓ Приложение перезапущено

═══════════════════════════════════════════════════════════════
  ПРОВЕРКА
═══════════════════════════════════════════════════════════════

1. Открыть браузер:
   http://localhost:8080

2. Картинки должны отображаться на главной странице

3. Проверить в БД:
   docker exec lab4-dev-postgres psql -U myuser -d credits \
     -c "SELECT id, title, image_url FROM credits LIMIT 3;"

═══════════════════════════════════════════════════════════════

